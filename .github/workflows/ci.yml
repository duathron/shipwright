name: CI
on:
  push:
  pull_request:

jobs:
  ci:
    # Dogfood: Shipwright's CI runs the framework's own reusable workflow.
    uses: ./.github/workflows/python-ci.yml
    with:
      run-build: true
